import Button from './Button'
import Alert from './Alert'

let components = [Button,Alert]

export default function(app){
	// app 是vue的实例对象
	// app.component('FlButton',Button)
	// app.component('FlAlert',Alert)
}